apiVersion: v1
kind: Service
metadata:
  name: admincluster
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: admincluster
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: admincluster
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 2
      maxSurge: 2
  selector:
    matchLabels:
      app: admincluster
  template:
    metadata:
      labels:
        app: admincluster
    spec:
      containers:
        - name: admincluster
          image: CONTAINER_IMAGE
          securityContext:
            privileged: false
            readOnlyRootFilesystem: false
            allowPrivilegeEscalation: false
          ports:
            - containerPort: 8080
